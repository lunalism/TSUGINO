import SwiftUI

/// Phase 0 placeholder root screen. Intentionally contains no product UI.
struct AppShellView: View {
    var body: some View {
        Text("TSUGINO")
            .font(.largeTitle)
            .fontWeight(.semibold)
    }
}
